<div class="text-center">
        @if (Model.Marque == "Tesla")
        {
            <div>electrique</div>
        }
        else
        {
            <div>diesel</div>
        }
        Voiture : @Model.Marque
    </div>