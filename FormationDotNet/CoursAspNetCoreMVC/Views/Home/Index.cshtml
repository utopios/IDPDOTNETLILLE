@model Voiture
@{
    ViewData["Title"] = "Home Page";
    Layout = "_Layout";
    //var voiture = @ViewData["voiture"] as Voiture;
}

@*<div class="text-center">
    @if(voiture.Marque == "Tesla")
    {
        <div>electrique</div>
    }else
    {
        <div>diesel</div>
    }
    Voiture : @voiture.Marque
</div>*@
@*
@foreach (Voiture voiture in (ViewData["voitures"] as List<Voiture>))
{
    <div class="text-center">
        @if (voiture.Marque == "Tesla")
        {
            <div>electrique</div>
        }
        else
        {
            <div>diesel</div>
        }
        Voiture : @voiture.Marque
    </div>
}*@

@foreach (Voiture voiture in ViewBag.voitures)
{
    <div class="text-center">
        @if (voiture.Marque == "Tesla")
        {
            <div>electrique</div>
        }
        else
        {
            <div>diesel</div>
        }
        Voiture : @voiture.Marque
        <a asp-controller="Home" asp-action="DetailVoiture" asp-route-marque="@voiture.Marque" >Lien</a>
    </div>
}
@*<div class="text-center">
        @if (Model.Marque == "Tesla")
        {
            <div>electrique</div>
        }
        else
        {
            <div>diesel</div>
        }
        Voiture : @Model.Marque
    </div>*@

<div>
    <form asp-action="SubmitForm" asp-controller="Home" method="post">
        <div>
            <input type="text" name="marque" />
        </div>
        <div>
            <button type="submit">Valider</button>
        </div>
    </form>
</div>